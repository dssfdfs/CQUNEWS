# 《校园跑腿服务平台》接口文档

**v1.0**

---

## 目录

- 一、接口规范说明
- 二、用户模块接口
- 三、任务模块接口
- 四、钱包模块接口
- 五、认证模块接口
- 六、评价模块接口
- 七、纠纷模块接口
- 八、管理模块接口

---

## 一、接口规范说明

### 1、基础URL

- 开发环境：`http://10.2.0.247:8081`

### 2、请求头

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| X-User-Id | Long | 是 | 用户ID |
| X-User-Openid | String | 是 | 用户OpenID |
| Content-Type | String | 是 | application/json |

### 3、响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

### 4、状态码说明

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

## 二、用户模块接口

### 1、微信登录

| 项目 | 说明 |
|------|------|
| 编号 | API001 |
| 页面 | 登录页面 |
| 功能描述 | 用户通过微信授权登录 |
| 请求URL | POST /users/login |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "code": "微信登录code"
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "openid": "oXXXX",
    "nickName": "用户昵称",
    "avatarUrl": "头像URL",
    "token": "JWT Token"
  }
}
```

失败：
```json
{
  "code": 400,
  "message": "登录失败",
  "data": null
}
```

---

### 2、获取用户信息

| 项目 | 说明 |
|------|------|
| 编号 | API002 |
| 页面 | 个人中心 |
| 功能描述 | 获取当前登录用户信息 |
| 请求URL | GET /users/info |
| 完成情况 | 已完成 |

**请求参数**

无（通过Header传递用户ID）

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "userId": 1,
    "nickName": "用户昵称",
    "avatarUrl": "头像URL",
    "phone": "13800138000",
    "creditScore": 100,
    "creditLevel": "good",
    "certificationStatus": "approved"
  }
}
```

---

### 3、更新用户信息

| 项目 | 说明 |
|------|------|
| 编号 | API003 |
| 页面 | 个人中心 |
| 功能描述 | 更新用户昵称、头像等信息 |
| 请求URL | PUT /users/info |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "nickName": "新昵称",
  "avatarUrl": "新头像URL"
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

## 三、任务模块接口

### 1、获取任务列表

| 项目 | 说明 |
|------|------|
| 编号 | API101 |
| 页面 | 首页 |
| 功能描述 | 获取任务列表，支持按类型筛选 |
| 请求URL | GET /tasks |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | String | 否 | 任务类型：express/shopping/document/food/print/other |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 1,
      "taskNo": "TASK1718123456789",
      "taskType": "express",
      "typeText": "代取快递",
      "title": "帮忙取一下菜鸟驿站的快递",
      "description": "取件码：12-3-4567",
      "fromLocation": "菜鸟驿站",
      "toLocation": "学生宿舍1栋",
      "reward": 5.00,
      "taskStatus": "pending",
      "publisherName": "张三",
      "createTime": "2024-06-18 10:00:00"
    }
  ]
}
```

---

### 2、获取任务详情

| 项目 | 说明 |
|------|------|
| 编号 | API102 |
| 页面 | 任务详情 |
| 功能描述 | 获取任务详细信息 |
| 请求URL | GET /tasks/{taskId} |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Long | 是 | 任务ID（路径参数） |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": 1,
    "taskNo": "TASK1718123456789",
    "taskType": "express",
    "typeText": "代取快递",
    "title": "帮忙取一下菜鸟驿站的快递",
    "description": "取件码：12-3-4567，比较大件",
    "fromLocation": "菜鸟驿站",
    "toLocation": "学生宿舍1栋302",
    "fromLatitude": 30.123456,
    "fromLongitude": 120.123456,
    "toLatitude": 30.234567,
    "toLongitude": 120.234567,
    "expectTime": "2024-06-18 18:00:00",
    "taskMode": "fixed",
    "reward": 5.00,
    "finalReward": 5.00,
    "taskStatus": "pending",
    "publisherName": "张三",
    "acceptorName": null,
    "acceptTime": null,
    "viewCount": 10,
    "createTime": "2024-06-18 10:00:00"
  }
}
```

---

### 3、发布任务

| 项目 | 说明 |
|------|------|
| 编号 | API103 |
| 页面 | 发布任务 |
| 功能描述 | 发布新任务 |
| 请求URL | POST /tasks |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "taskType": "express",
  "description": "帮忙取一下菜鸟驿站的快递，取件码：12-3-4567",
  "fromLocation": "菜鸟驿站",
  "toLocation": "学生宿舍1栋302",
  "fromLatitude": 30.123456,
  "fromLongitude": 120.123456,
  "toLatitude": 30.234567,
  "toLongitude": 120.234567,
  "expectTime": "2024-06-18 18:00:00",
  "taskMode": "fixed",
  "reward": 5.00,
  "images": ["image1.jpg", "image2.jpg"]
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "任务发布成功",
  "data": {
    "id": 1,
    "taskNo": "TASK1718123456789",
    "taskStatus": "pending"
  }
}
```

失败（余额不足）：
```json
{
  "code": 400,
  "message": "余额不足，请先充值",
  "data": null
}
```

---

### 4、接单

| 项目 | 说明 |
|------|------|
| 编号 | API104 |
| 页面 | 任务详情 |
| 功能描述 | 接受任务（一口价模式） |
| 请求URL | POST /tasks/{taskId}/accept |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Long | 是 | 任务ID（路径参数） |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "接单成功",
  "data": null
}
```

失败：
```json
{
  "code": 400,
  "message": "任务已被他人接单",
  "data": null
}
```

---

### 5、更新任务状态

| 项目 | 说明 |
|------|------|
| 编号 | API105 |
| 页面 | 我的接单 |
| 功能描述 | 更新任务状态（取件/送达） |
| 请求URL | POST /tasks/{taskId}/status |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "status": "picked"
}
```

状态值说明：
- `picked`：已取件
- `delivered`：已送达

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "状态更新成功",
  "data": null
}
```

---

### 6、确认送达

| 项目 | 说明 |
|------|------|
| 编号 | API106 |
| 页面 | 任务历史 |
| 功能描述 | 发布者确认收到物品 |
| 请求URL | POST /tasks/{taskId}/confirm-delivery |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Long | 是 | 任务ID（路径参数） |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "确认成功，资金已到账",
  "data": null
}
```

---

### 7、取消任务

| 项目 | 说明 |
|------|------|
| 编号 | API107 |
| 页面 | 任务详情 |
| 功能描述 | 取消任务 |
| 请求URL | POST /tasks/{taskId}/cancel |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Long | 是 | 任务ID（路径参数） |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "任务已取消",
  "data": null
}
```

---

### 8、提交评价

| 项目 | 说明 |
|------|------|
| 编号 | API108 |
| 页面 | 任务历史 |
| 功能描述 | 发布者对接单者进行评价 |
| 请求URL | POST /tasks/{taskId}/review |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "deliveryTimeRating": 5,
  "serviceQualityRating": 4,
  "reviewContent": "服务很好，送达准时"
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "评价成功",
  "data": null
}
```

---

### 9、获取用户发布的任务

| 项目 | 说明 |
|------|------|
| 编号 | API109 |
| 页面 | 我的发布 |
| 功能描述 | 获取当前用户发布的任务列表 |
| 请求URL | GET /tasks/user/published |
| 完成情况 | 已完成 |

**请求参数**

无（通过Header传递用户ID）

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 1,
      "taskNo": "TASK1718123456789",
      "title": "帮忙取快递",
      "taskStatus": "completed",
      "reward": 5.00,
      "createTime": "2024-06-18 10:00:00"
    }
  ]
}
```

---

### 10、获取用户参与的任务

| 项目 | 说明 |
|------|------|
| 编号 | API110 |
| 页面 | 任务历史 |
| 功能描述 | 获取用户参与过的所有任务（发布或接单） |
| 请求URL | GET /tasks/user/participated |
| 完成情况 | 已完成 |

**请求参数**

无（通过Header传递用户ID）

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 1,
      "taskNo": "TASK1718123456789",
      "title": "帮忙取快递",
      "taskStatus": "completed",
      "reward": 5.00,
      "isPublisher": true,
      "publisherName": "张三",
      "acceptorName": "李四",
      "createTime": "2024-06-18 10:00:00"
    }
  ]
}
```

---

## 四、钱包模块接口

### 1、获取钱包余额

| 项目 | 说明 |
|------|------|
| 编号 | API201 |
| 页面 | 钱包 |
| 功能描述 | 获取用户钱包余额和统计信息 |
| 请求URL | GET /wallet/balance |
| 完成情况 | 已完成 |

**请求参数**

无（通过Header传递用户ID）

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "balance": 100.00,
    "frozenAmount": 10.00,
    "totalIncome": 500.00,
    "totalExpense": 400.00
  }
}
```

---

### 2、充值

| 项目 | 说明 |
|------|------|
| 编号 | API202 |
| 页面 | 钱包 |
| 功能描述 | 通过微信支付充值 |
| 请求URL | POST /wallet/recharge |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "amount": 100.00
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "充值成功",
  "data": {
    "transactionNo": "TXN1718123456789",
    "balance": 200.00
  }
}
```

---

### 3、获取交易记录

| 项目 | 说明 |
|------|------|
| 编号 | API203 |
| 页面 | 钱包 |
| 功能描述 | 获取用户交易记录列表 |
| 请求URL | GET /wallet/transactions |
| 完成情况 | 已完成 |

**请求参数**

无（通过Header传递用户ID）

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 1,
      "transactionNo": "TXN1718123456789",
      "transactionType": "recharge",
      "amount": 100.00,
      "balanceAfter": 200.00,
      "description": "账户充值",
      "createTime": "2024-06-18 10:00:00"
    }
  ]
}
```

---

## 五、认证模块接口

### 1、提交实名认证

| 项目 | 说明 |
|------|------|
| 编号 | API301 |
| 页面 | 实名认证 |
| 功能描述 | 提交实名认证申请 |
| 请求URL | POST /certifications |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "realName": "张三",
  "idCard": "110101199001011234",
  "phone": "13800138000",
  "school": "XX大学",
  "studentId": "2021001",
  "idCardFront": "front.jpg",
  "idCardBack": "back.jpg",
  "studentCard": "student.jpg"
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "认证申请已提交，等待审核",
  "data": null
}
```

---

### 2、获取认证状态

| 项目 | 说明 |
|------|------|
| 编号 | API302 |
| 页面 | 个人中心 |
| 功能描述 | 获取当前用户认证状态 |
| 请求URL | GET /certifications/status |
| 完成情况 | 已完成 |

**请求参数**

无（通过Header传递用户ID）

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "certificationStatus": "approved",
    "realName": "张三",
    "school": "XX大学"
  }
}
```

---

## 六、评价模块接口

### 1、获取任务评价

| 项目 | 说明 |
|------|------|
| 编号 | API401 |
| 页面 | 任务详情 |
| 功能描述 | 获取任务的评价信息 |
| 请求URL | GET /reviews/task/{taskId} |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| taskId | Long | 是 | 任务ID（路径参数） |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "rating": 5,
    "deliveryTimeRating": 5,
    "serviceQualityRating": 5,
    "reviewContent": "服务很好",
    "createTime": "2024-06-18 12:00:00"
  }
}
```

---

## 七、纠纷模块接口

### 1、申请纠纷

| 项目 | 说明 |
|------|------|
| 编号 | API501 |
| 页面 | 任务详情 |
| 功能描述 | 申请纠纷仲裁 |
| 请求URL | POST /disputes |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "taskId": 1,
  "disputeReason": "物品损坏",
  "disputeImages": ["image1.jpg"]
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "纠纷申请已提交",
  "data": null
}
```

---

## 八、管理模块接口

### 1、管理员登录

| 项目 | 说明 |
|------|------|
| 编号 | API601 |
| 页面 | 管理登录 |
| 功能描述 | 管理员登录 |
| 请求URL | POST /admin/login |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "username": "admin",
  "password": "123456"
}
```

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "adminId": 1,
    "username": "admin",
    "token": "JWT Token"
  }
}
```

---

### 2、获取用户列表

| 项目 | 说明 |
|------|------|
| 编号 | API602 |
| 页面 | 用户管理 |
| 功能描述 | 获取用户列表 |
| 请求URL | GET /admin/users |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| size | Integer | 否 | 每页数量，默认10 |
| keyword | String | 否 | 搜索关键词 |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "records": [
      {
        "id": 1,
        "nickName": "张三",
        "phone": "13800138000",
        "creditScore": 100,
        "certificationStatus": "approved",
        "createTime": "2024-06-18 10:00:00"
      }
    ],
    "total": 100,
    "current": 1,
    "size": 10
  }
}
```

---

### 3、审核认证

| 项目 | 说明 |
|------|------|
| 编号 | API603 |
| 页面 | 认证管理 |
| 功能描述 | 审核实名认证申请 |
| 请求URL | PUT /admin/certifications/{certId}/audit |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "status": "approved",
  "rejectReason": ""
}
```

状态值说明：
- `approved`：通过
- `rejected`：拒绝

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "审核成功",
  "data": null
}
```

---

### 4、获取纠纷列表

| 项目 | 说明 |
|------|------|
| 编号 | API604 |
| 页面 | 纠纷管理 |
| 功能描述 | 获取纠纷列表 |
| 请求URL | GET /admin/disputes |
| 完成情况 | 已完成 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| status | String | 否 | 纠纷状态：pending/processing/resolved/closed |

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 1,
      "taskId": 1,
      "taskNo": "TASK1718123456789",
      "title": "帮忙取快递",
      "reason": "物品损坏",
      "status": "pending",
      "createTime": "2024-06-18 10:00:00"
    }
  ]
}
```

---

### 5、处理纠纷

| 项目 | 说明 |
|------|------|
| 编号 | API605 |
| 页面 | 纠纷管理 |
| 功能描述 | 处理纠纷 |
| 请求URL | PUT /admin/disputes/{disputeId}/handle |
| 完成情况 | 已完成 |

**请求参数**

```json
{
  "result": "support_user",
  "description": "经核实，物品确实损坏，资金退回发布者"
}
```

结果值说明：
- `support_user`：支持发布者
- `support_runner`：支持接单者
- `negotiate`：协商解决

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "处理成功",
  "data": null
}
```

---

### 6、获取统计数据

| 项目 | 说明 |
|------|------|
| 编号 | API606 |
| 页面 | 首页 |
| 功能描述 | 获取平台统计数据 |
| 请求URL | GET /admin/statistics |
| 完成情况 | 已完成 |

**请求参数**

无

**响应数据**

成功：
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "totalUsers": 1000,
    "totalTasks": 5000,
    "totalTransactions": 10000.00,
    "pendingCertifications": 10,
    "pendingDisputes": 5
  }
}
```

---

## 附录：任务状态说明

| 状态值 | 说明 |
|--------|------|
| pending | 待接单 |
| accepted | 已接单 |
| picked | 已取件 |
| delivered | 已送达 |
| completed | 已完成 |
| cancelled | 已取消 |

## 附录：任务类型说明

| 类型值 | 说明 |
|--------|------|
| express | 代取快递 |
| shopping | 代购物品 |
| document | 文件传递 |
| food | 代买餐饮 |
| print | 代打印 |
| other | 其他 |

## 附录：认证状态说明

| 状态值 | 说明 |
|--------|------|
| unsubmitted | 未提交 |
| pending | 审核中 |
| approved | 已通过 |
| rejected | 已拒绝 |

## 附录：纠纷状态说明

| 状态值 | 说明 |
|--------|------|
| pending | 待处理 |
| processing | 处理中 |
| resolved | 已解决 |
| closed | 已关闭 |
